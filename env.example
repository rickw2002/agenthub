# Database
# For Supabase: Both URLs should be identical, use direct connection (port 5432) with sslmode=require
# IMPORTANT: If password contains special characters (!@#$%^&*), URL encode them:
#   ! = %21, @ = %40, # = %23, $ = %24, % = %25, & = %26, * = %2A
# Example: password "abc!@#" becomes "abc%21%40%23"
DATABASE_URL="postgresql://postgres:<password>@db.<project-id>.supabase.co:5432/postgres?schema=public&sslmode=require"
DIRECT_URL="postgresql://postgres:<password>@db.<project-id>.supabase.co:5432/postgres?schema=public&sslmode=require"

# Supabase Storage
SUPABASE_URL="https://<project-id>.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"

# Agent Service (for n8n callbacks)
AGENT_SERVICE_KEY="change-this-to-a-random-secret-key-for-service-callbacks"

# Agent Service Key (public, for local testing only - DO NOT USE IN PRODUCTION)
NEXT_PUBLIC_AGENT_SERVICE_KEY="change-this-to-same-value-as-AGENT_SERVICE_KEY-for-local-testing"

# Intel Service (FastAPI) - OAuth & Data Sync
NEXT_PUBLIC_INTEL_BASE_URL="http://localhost:8001"
# Server-side API key for proxying requests to FastAPI (not exposed to client)
INTEL_API_KEY="dev-key"
# Secret for triggering cron jobs (must match Intel service CRON_SECRET)
CRON_SECRET="dev-secret"

# Google Analytics 4
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"


